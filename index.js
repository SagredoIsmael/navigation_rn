import 'react-native-gesture-handler'
import { NavigationContainer } from '@react-navigation/native'


const NavigationWrapper = ({App}) => {
  <NavigationContainer>
     <App/>
  </NavigationContainer>
}

export default NavigationWrapper